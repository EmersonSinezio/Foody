import ProductCard from "../components/ProductCard";

const Products = () => {
  return (
    <div className="flex items-center justify-center flex-col bg-slate-50 h-[90vh]">
      <ProductCard />
    </div>
  );
};

export default Products;
